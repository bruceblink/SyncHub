# Global Codex Workflow

- 每完成一个功能，先运行与该功能风险相匹配的测试或验证命令；测试通过后再提交代码。
- 一个已完成的小功能对应一次提交，避免把多个无关功能批量放进同一个提交。
- 如果功能尚不可测试、验证失败，或只完成了一部分，不要把它作为完成状态提交。
- 提交代码时忽略 GPG 签名，使用 `git commit --no-gpg-sign`，并保持本仓库 `commit.gpgsign=false`。
